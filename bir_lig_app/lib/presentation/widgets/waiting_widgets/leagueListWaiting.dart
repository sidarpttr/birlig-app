import 'package:flutter/material.dart';

class LeagueListWaiting extends StatelessWidget {
  const LeagueListWaiting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
        child: Padding(
          padding: EdgeInsets.symmetric(horizontal: 10),
          child: ListTile(
            title: Text(""),
          ),
        ),
      );
  }
}